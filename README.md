# pg-assets

Prebuilt PostgreSQL extension binaries (pgvector) for fabmem's embedded Postgres, per platform.
Built by `.github/workflows/build-pgvector.yml`; consumed by `src/db/pg-provision.ts` (downloadPgvector).

> Test host for fabmem. Will be relocated to a dedicated org later.
